'use strict';

const favArtists = ['Jo', 'Harry', 'Bob', 'Medusa', 'George Strait'];

const grabFromArray = function(someArray) {
    // console.log(`${someArray.at(0)}, ${someArray.at(2)}, and ${someArray.at(someArray.length - 1)}`);
    // console.log(`${someArray.shift()}, ${someArray.slice(1,2)}, and ${someArray.pop()}`);
}
grabFromArray(favArtists);